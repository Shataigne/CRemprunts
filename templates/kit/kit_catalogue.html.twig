{% extends 'base.html.twig' %}

{% block title %}Kit de simulation{% endblock %}
{% block body %}
    <div class="catalogue_container">
        <div class="catalogue_filtres">
            <h2>Filtres :</h2>
            {{ form_start(form) }}
            {{form_row(form.q) }}
            {{form_row(form.centres) }}
            {{form_row(form.dispoNow) }}
            <button>Filtrer</button>
            {{ form_end(form) }}
        </div>

        <div>
            <div>
            {% if kits is not empty %}
        {% for kit in kits %}
            <a href="{{ path('app_kit_details', {'id': kit.id}) }}">
                <div class="catalogue_card">
                    <h2>{{ kit.libelle }}</h2>
                    {{ kit.centre.libelle }}
                </div>
            </a>

        {% endfor %}
            {% else %}
                <div class="catalogue_card">
                    Rien ne correspond à votre recherche !
                </div>
            {% endif %}
            </div>
        </div>
    </div>
{% endblock %}